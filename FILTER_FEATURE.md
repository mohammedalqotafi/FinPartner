# ✨ ميزة الفلترة في صفحة التفاصيل المالية

## 📋 الوصف

تم إضافة ميزة فلترة ذكية لصفحة التفاصيل المالية للعضو. الآن عند الضغط على أي بطاقة في صفحة تفاصيل العضو، ستظهر فقط البيانات المتعلقة بتلك البطاقة.

---

## 🎯 كيف تعمل؟

### البطاقات وأنواع الفلترة:

| البطاقة | نوع الفلتر | ما يُعرض |
|---------|-----------|----------|
| **الرصيد الموحد النهائي** | `?filter=all` | جميع البيانات (الإيداعات + السحوبات + المصروفات) |
| **إجمالي الإيداعات** | `?filter=deposits` | الإيداعات فقط |
| **إجمالي السحوبات** | `?filter=withdrawals` | السحوبات فقط |
| **إجمالي المصروفات** | `?filter=expenses` | المصروفات فقط (المدفوعة + المستحقة) |
| **المصروفات المدفوعة** | `?filter=expenses` | المصروفات فقط |
| **صافي التغير** | `?filter=all` | جميع البيانات |

---

## 📱 مثال الاستخدام

### السيناريو 1: عرض المصروفات فقط

1. افتح صفحة تفاصيل عضو: `http://localhost:5173/members/1`
2. اضغط على بطاقة **"إجمالي المصروفات"**
3. ستنتقل إلى: `http://localhost:5173/members/1/financials?filter=expenses`
4. ستظهر فقط:
   - بطاقات الملخص (في الأعلى)
   - المصروفات المدفوعة
   - المصروفات المستحقة
5. لن تظهر: الإيداعات والسحوبات

### السيناريو 2: عرض الإيداعات فقط

1. اضغط على بطاقة **"إجمالي الإيداعات"**
2. ستنتقل إلى: `http://localhost:5173/members/1/financials?filter=deposits`
3. ستظهر فقط:
   - بطاقات الملخص
   - قائمة الإيداعات التفصيلية
4. لن تظهر: السحوبات والمصروفات

### السيناريو 3: عرض كل شيء

1. اضغط على بطاقة **"الرصيد الموحد النهائي"**
2. أو اضغط على زر **"عرض الكل"** (يظهر عند تطبيق فلتر)
3. ستنتقل إلى: `http://localhost:5173/members/1/financials?filter=all`
4. ستظهر جميع البيانات

---

## 🎨 التحسينات المضافة

### 1. عنوان ديناميكي
العنوان يتغير حسب نوع الفلتر:
- `💰 الإيداعات` - عند فلترة الإيداعات
- `💸 السحوبات` - عند فلترة السحوبات
- `🧾 المصروفات` - عند فلترة المصروفات
- `التفاصيل المالية` - عند عرض الكل

### 2. زر "عرض الكل"
- يظهر فقط عند تطبيق فلتر معين
- يسمح بالعودة لعرض جميع البيانات بسرعة
- موجود في الزاوية العلوية اليمنى

### 3. زر العودة الذكي
- يعيدك إلى صفحة تفاصيل العضو (وليس قائمة الأعضاء)
- يحافظ على سياق التصفح

---

## 🔧 التفاصيل التقنية

### Query Parameters:
```typescript
?filter=all        // عرض الكل (افتراضي)
?filter=deposits   // الإيداعات فقط
?filter=withdrawals // السحوبات فقط
?filter=expenses   // المصروفات فقط
```

### الكود المستخدم:

#### في MemberDetailPage.tsx:
```typescript
// بطاقة المصروفات
onClick={() => navigate(`/members/${id}/financials?filter=expenses`)}

// بطاقة الإيداعات
onClick={() => navigate(`/members/${id}/financials?filter=deposits`)}

// بطاقة السحوبات
onClick={() => navigate(`/members/${id}/financials?filter=withdrawals`)}
```

#### في MemberFinancialsPage.tsx:
```typescript
const [searchParams] = useSearchParams();
const filterType = searchParams.get('filter') || 'all';

// عرض الإيداعات فقط إذا كان الفلتر 'all' أو 'deposits'
{(filterType === 'all' || filterType === 'deposits') && financials.deposits.count > 0 && (
  <div>...</div>
)}

// عرض المصروفات فقط إذا كان الفلتر 'all' أو 'expenses'
{(filterType === 'all' || filterType === 'expenses') && financials.expenses.paid.count > 0 && (
  <div>...</div>
)}
```

---

## ✅ الفوائد

1. **تجربة مستخدم أفضل**: المستخدم يرى فقط ما يهمه
2. **تصفح أسرع**: لا حاجة للتمرير عبر جميع البيانات
3. **وضوح أكبر**: التركيز على نوع واحد من البيانات
4. **مرونة**: يمكن العودة لعرض الكل بسهولة

---

## 🎯 أمثلة URLs

```
# عرض الكل
http://localhost:5173/members/1/financials
http://localhost:5173/members/1/financials?filter=all

# الإيداعات فقط
http://localhost:5173/members/1/financials?filter=deposits

# السحوبات فقط
http://localhost:5173/members/1/financials?filter=withdrawals

# المصروفات فقط
http://localhost:5173/members/1/financials?filter=expenses
```

---

## 📸 ما يجب أن تراه

### عند الضغط على "إجمالي المصروفات":

```
┌─────────────────────────────────────────────┐
│  [←] 🧾 المصروفات              [عرض الكل]   │
│      👤 أحمد محمود                          │
└─────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│  💰 الرصيد الصافي: 3,916.66 ر.س            │
│  📈 الإيداعات: 5,000 ر.س                   │
│  📉 السحوبات: 1,000 ر.س                    │
│  🧾 دفعت للآخرين: 0 ر.س                    │
│  📝 مستحق عليك: 83.34 ر.س                  │
└──────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│  🧾 المصروفات المدفوعة (0)                  │
│  (لا توجد مصروفات مدفوعة)                   │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│  📝 المصروفات المستحقة (2)                  │
│  إجمالي المستحق: 83.34 ر.س                 │
├─────────────────────────────────────────────┤
│  [📝] EXP-0001 | طعام | 41.67 ر.س          │
│  [📝] EXP-0002 | مواصلات | 41.67 ر.س       │
└─────────────────────────────────────────────┘
```

**لاحظ**: لا تظهر أقسام الإيداعات والسحوبات!

---

## 🚀 التشغيل

```bash
# Terminal 1: Backend
cd backend
php artisan serve

# Terminal 2: Frontend
cd frontend
npm run dev

# Browser
http://localhost:5173/members
```

ثم:
1. اضغط على أي عضو
2. اضغط على بطاقة "إجمالي المصروفات"
3. ستظهر المصروفات فقط! 🎉

---

## 📝 ملاحظات

- بطاقات الملخص (Summary Cards) تظهر دائماً في الأعلى
- الفلترة تؤثر فقط على الأقسام التفصيلية
- يمكن تغيير الفلتر بالضغط على "عرض الكل" أو العودة للصفحة السابقة

---

## ✨ الخلاصة

الآن عند الضغط على بطاقة "إجمالي المصروفات"، ستظهر فقط تفاصيل المصروفات بدون الإيداعات والسحوبات! 🎊
